# Overridable at deploy time (-M-mule.env.<key>=<value>, or a per-environment properties file).
listener.port=8082

# Oracle connection (the only supported database — see pom.xml's file comment).
db.host=localhost
db.port=1521
db.sid=XE
db.user=system
db.password=Oracle_123
